name: Text-to-SQL eval (nightly)

# Scored runs cost money and need credentials, so they are kept out of the PR
# path. CI runs the free half (harness self-test + golden-set verification) on
# every pull request; this job produces the accuracy numbers.
#
# Default provider is `openai` — the same matrix the app ships. GitHub Models
# was retired in July 2026; `--provider github` is refused with that reason.
# Override via workflow_dispatch (openai / openai-compatible / ollama).
# `openai-compatible` needs repo secrets OPENAI_BASE_URL and usually
# OPENAI_COMPAT_API_KEY (or OPENAI_API_KEY).

on:
  schedule:
    - cron: '17 3 * * *'
  workflow_dispatch:
    inputs:
      provider:
        description: 'openai | openai-compatible | ollama'
        required: false
        default: 'openai'
      model:
        description: 'Model override (blank uses the provider default)'
        required: false
        default: ''

permissions:
  contents: read

jobs:
  sqlite:
    name: SQLite · ${{ github.event.inputs.provider || 'openai' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - run: npm install

      - name: Run eval
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_COMPAT_API_KEY: ${{ secrets.OPENAI_COMPAT_API_KEY }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
        run: |
          npm run eval -- \
            --provider "${{ github.event.inputs.provider || 'openai' }}" \
            ${{ github.event.inputs.model && format('--model "{0}"', github.event.inputs.model) || '' }} \
            --dialect sqlite \
            --tag "nightly-sqlite"

      - uses: actions/upload-artifact@v4
        with:
          name: eval-sqlite
          path: evals/results/

  postgres:
    name: PostgreSQL · ${{ github.event.inputs.provider || 'openai' }}
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: litedb_eval
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - run: npm install

      - name: Run eval
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_COMPAT_API_KEY: ${{ secrets.OPENAI_COMPAT_API_KEY }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          EVAL_POSTGRES_URL: postgres://postgres:postgres@localhost:5432/litedb_eval
        run: |
          npm run eval -- \
            --provider "${{ github.event.inputs.provider || 'openai' }}" \
            ${{ github.event.inputs.model && format('--model "{0}"', github.event.inputs.model) || '' }} \
            --dialect postgres \
            --tag "nightly-postgres"

      - uses: actions/upload-artifact@v4
        with:
          name: eval-postgres
          path: evals/results/
